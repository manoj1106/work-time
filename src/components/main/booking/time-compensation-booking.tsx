import { Fragment } from 'react';

interface TimeCompensationBookingProps {}
const TimeCompensationBooking = (props: TimeCompensationBookingProps) => {
  return <Fragment>Time Compensation Booking</Fragment>;
};
export default TimeCompensationBooking;
