import Card from '@/components/widgets/card/card';

const headers = {
  title: 'My Booking',
  subtitle: 'All your bookings',
};

const MyBookings = () => {
  return <Card headers={headers}></Card>;
};
export default MyBookings;
